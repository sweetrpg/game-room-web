---
kind: Service
apiVersion: v1

metadata:
  name: web

spec:
  selector:
    app: library
    component: web
    platform: sweetrpg
  type: LoadBalancer
  ports:
    - name: http
      port: 8282
      targetPort: http
