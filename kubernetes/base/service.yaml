---
kind: Service
apiVersion: v1

metadata:
    name: web-v1

spec:
    selector:
        app: web
        version: v1
        platform: sweetrpg
        package: game-room
        component: web
    type: LoadBalancer
    ports:
        - name: http
          port: 8282
          targetPort: http
