---
apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: sweetrpg-library-web

spec:
  ingressClassName: traefik
  rules:
    - host: library.sweetrpg.com
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: sweetrpg-library-web
                port:
                  number: 8280
