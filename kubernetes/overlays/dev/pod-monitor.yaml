---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor

metadata:
  name: sweetrpg-game-room-web
  labels:
    release: prometheus

spec:
  podMetricsEndpoints:
    - port: http
  selector:
    matchLabels:
        platform: sweetrpg
        app: web
        package: game-room
        component: web
  namespaceSelector:
    matchNames:
      - sweetrpg-game-room
